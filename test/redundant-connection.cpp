#include "iostream"
#include "helper.h"
#include "map"

class Solution {
public:
    vector<int> findRedundantConnection(vector<vector<int>>& edges) {
        
    }
};

int main()
{

    Solution s;
    vector<int> st = {1, 2, 3};
    s.permute(st);
}
