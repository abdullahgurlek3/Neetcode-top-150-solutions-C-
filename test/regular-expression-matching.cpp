#include "iostream"
#include "helper.h"
#include "map"

class Solution
{
public:
    bool isMatch(string s, string p)
    {
        bool deep(s, p);
    }
    bool deep(string s, string p)
    {
        if (p == "")
            return false;
        
    }
};

int main()
{

    Solution s;
    cout << s.isMatch("a", "*..");
}
